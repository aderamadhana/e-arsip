  <!-- Content Wrapper. Contains page content -->
  <div class="content-wrapper">
    <!-- Content Header (Page header) -->
    <section class="content-header">
      <h1>
        Sponsorship
        <small>Master</small>
      </h1>
      <ol class="breadcrumb">
        <li><a href="#"><i class="fa fa-dashboard"></i> Home</a></li>
        <li><a href="#">master</a></li>
        <li class="active">Sponsorship</li>
      </ol>
    </section>
    <!-- Main content -->
    <section class="content">
    <form method="POST" action="<?= base_url('master/Sponsorship/update') ?>" id="upload-create" enctype="multipart/form-data">
    <input type="hidden" name="id" value="<?= $sponsorship['id'] ?>">
      <div class="row">
      <div class="col-xs-2">
          <a href="<?= base_url('master/Sponsorship/') ?>" class="btn btn-block btn-default"> < < Kembali ke List Sponsor </a>
        </div>
        <div class="col-xs-10">
          <div class="box box-primary">
            <!-- /.box-header -->
           
            <div class="box-body">
                <div class="show_error"></div>
                  <div class="row">
                    <div class="col-md-6">
                      <div class="form-group">
                          <label for="form-nama_kegiatan">Nama kegiatan</label>
                          <input type="text" class="form-control" id="form-nama_kegiatan" placeholder="Masukan Nama kegiatan" name="dt[nama_kegiatan]" value="<?= $sponsorship['nama_kegiatan'] ?>">
                      </div>

                      <div class="form-group">
                          <label for="form-tanggal">Tanggal</label>
                          <input type="text" class="form-control tgl" id="form-tanggal" placeholder="Masukan Tanggal" name="dt[tanggal]" value="<?= $sponsorship['tanggal'] ?>">
                      </div>

                      <div class="form-group">
                        <label for="form-klasifikasi_id">Klasifikasi</label>
                        <select name="dt[klasifikasi_id]" class="form-control select2" style="width:100%">
                          <option value="0">Pilih</option>
                          <?php 
                          $klasifikasi = $this->mymodel->selectWhere('klasifikasi',null);
                          foreach ($klasifikasi as $klasifikasi_record) {
                          $text="";
                          if($klasifikasi_record['id']==$sponsorship['klasifikasi_id']){
                          $text = "selected";
                          }
                          echo "<option value=".$klasifikasi_record['id']." ".$text." >".$klasifikasi_record['klasifikasi']."</option>";
                          }
                          ?>
                        </select>
                      </div>

                      <div class="form-group">
                        <label for="form-lingkup_id">Lingkup</label>
                        <select name="dt[lingkup_id]" class="form-control select2" style="width:100%">
                          <option value="0">Pilih</option>
                          <?php 
                          $lingkup = $this->mymodel->selectWhere('lingkup',null);
                          foreach ($lingkup as $lingkup_record) {
                          $text="";
                          if($lingkup_record['id']==$sponsorship['lingkup_id']){
                          $text = "selected";
                          }
                          echo "<option value=".$lingkup_record['id']." ".$text." >".$lingkup_record['lingkup']."</option>";
                          }
                          ?>
                        </select>
                      </div>

                      <div class="form-group">
                        <label for="form-kantor_id">Kantor</label>
                        <select name="dt[kantor_id]" class="form-control select2" style="width:100%">
                          <option value="0">Pilih</option>
                          <?php 
                          $kantor = $this->mymodel->selectWhere('kantor',null);
                          foreach ($kantor as $kantor_record) {
                          $text="";
                          if($kantor_record['id']==$sponsorship['kantor_id']){
                          $text = "selected";
                          }
                          echo "<option value=".$kantor_record['id']." ".$text." >".$kantor_record['kantor']."</option>";
                          }
                          ?>
                        </select>
                      </div>

                      <div class="form-group">
                          <label for="form-penjelasan_kerjasama">Penjelasan kerjasama</label>
                          <textarea name="dt[penjelasan_kerjasama]" class="form-control"><?= $sponsorship['penjelasan_kerjasama'] ?></textarea>
                      </div>

                    </div>
                    <div class="col-md-6">
                      <div class="form-group">
                        <label for="form-keuntungan_id">Keuntungan</label>
                        <select name="dt[keuntungan_id]" class="form-control select2" style="width:100%">
                          <option value="0">Pilih</option>
                          <?php 
                          $keuntungan = $this->mymodel->selectWhere('keuntungan',null);
                          foreach ($keuntungan as $keuntungan_record) {
                          $text="";
                          if($keuntungan_record['id']==$sponsorship['keuntungan_id']){
                          $text = "selected";
                          }
                          echo "<option value=".$keuntungan_record['id']." ".$text." >".$keuntungan_record['keuntungan']."</option>";
                          }
                          ?>
                        </select>
                      </div>

                      <div class="form-group">
                          <label for="form-nominal_sponsor">Nominal sponsor</label>
                          <input type="text" class="form-control money" id="form-nominal_sponsor" placeholder="Masukan Nominal sponsor" name="dt[nominal_sponsor]" value="<?= $sponsorship['nominal_sponsor'] ?>">
                      </div>

                      <div class="form-group">
                          <label for="form-nominal_pajak_sponsor">Nominal pajak sponsor</label>
                          <input type="text" class="form-control money" id="form-nominal_pajak_sponsor" placeholder="Masukan Nominal pajak sponsor" name="dt[nominal_pajak_sponsor]" value="<?= $sponsorship['nominal_pajak_sponsor'] ?>">
                      </div>

                      <div class="form-group">
                        <label for="form-keterangan_id">Keterangan</label>
                        <select name="dt[keterangan_id]" class="form-control select2" style="width:100%">
                          <option value="0">Pilih</option>
                          <?php 
                          $keterangan = $this->mymodel->selectWhere('keterangan',null);
                          foreach ($keterangan as $keterangan_record) {
                          $text="";
                          if($keterangan_record['id']==$sponsorship['keterangan_id']){
                          $text = "selected";
                          }
                          echo "<option value=".$keterangan_record['id']." ".$text." >".$keterangan_record['keterangan']."</option>";
                          }
                          ?>
                        </select>
                      </div>
                    
                    </div>
                  </div>
          

                <?php
                  if($file['dir']!=""){
                    $types = explode("/", $file['mime']);
                  if($types[0]=="image"){
                    ?>
                    <img src="<?= base_url($file['dir']) ?>" style="width: 200px" class="img img-thumbnail">
                    <br>
                  <?php }else{ ?>

                    <i class="fa fa-file fa-5x text-danger"></i>
                    <br>
                    <a href="<?= base_url($file['dir']) ?>" target="_blank"><i class="fa fa-download"></i> <?= $file['name'] ?></a>
                    <br>
                  <br>
                <?php } ?>
              <?php } ?>
                <div class="form-group">
                    <label for="form-file">File</label>
                    <input type="file" class="" id="form-file" placeholder="Masukan File" name="file">
                </div>

          </div>
          <div class="box-footer">
              <button type="submit" class="btn btn-primary btn-send" ><i class="fa fa-save"></i> Save</button>
              <button type="reset" class="btn btn-danger"><i class="fa fa-refresh"></i> Reset</button>

          </div>
          <!-- /.box-body -->
        </div>
        <!-- /.box -->
        <!-- /.box -->
      </div>
      <!-- /.col -->
    </div>
    <!-- /.row -->
    </form>
  </section>
  <!-- /.content -->
</div>
<!-- /.content-wrapper -->
<script type="text/javascript">
tinymce.remove();
tinymce.init({
  selector: '.tinymces',
    cleanup : true,
  plugins: "localautosave",
  toolbar1: "localautosave",
  las_seconds: 15,
  las_nVersions: 15,
  las_keyName: "LocalAutoSave",
  las_callback: function() {
    var content = this.content; //content saved
    var time = this.time; //time on save action
  }
});

    $("#upload-create").submit(function(){
              var form = $(this);
            var mydata = new FormData(this);
            $.ajax({
                  type: "POST",
                url: form.attr("action"),
                data: mydata,
                cache: false,
                contentType: false,
                processData: false,
                beforeSend : function(){
                      $(".btn-send").addClass("disabled").html("<i class='la la-spinner la-spin'></i>  Processing...").attr('disabled',true);
                    //form.find(".show_error").slideUp().html("");
              },
              success: function(response, textStatus, xhr) {
                      // alert(mydata);
                   var str = response;
                    if (str.indexOf("success") != -1){
                          //form.find(".show_error").hide().html(response).slideDown("fast");
                        
                          Swal.fire({
                              title: "It works!",
                              text: "Successfully updated data",
                              icon: "success"
                          });
                          setTimeout(function(){ 
                             window.location.href = "<?= base_url('master/Sponsorship') ?>";
                      }, 1000);
                      $(".btn-send").removeClass("disabled").html('<i class="fa fa-save"></i> Save').attr('disabled',false);
                  }else{
                          Swal.fire({
                            title: "Oppss!",
                            html: str,
                            icon: "error"
                          });
                          // form.find(".show_error").hide().html(response).slideDown("fast");
                        $(".btn-send").removeClass("disabled").html('<i class="fa fa-save"></i> Save').attr('disabled',false);

                  }
              },
              error: function(xhr, textStatus, errorThrown) {
                    console.log(xhr);
                    Swal.fire({
                        title: "Oppss!",
                        text: xhr,
                        icon: "error"
                    });
                    $(".btn-send").removeClass("disabled").html('<i class="fa fa-save"></i> Save').attr('disabled',false);
                    // form.find(".show_error").hide().html(xhr).slideDown("fast");
              }
          });
          return false;

      });
</script>